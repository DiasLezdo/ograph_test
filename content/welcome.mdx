# Welcome MDX Post

<Callout type="success">
  This post is loaded with:
  <br />
  <code>const Post = await import(`@/content/${slug}.mdx`)</code>
</Callout>

This proves your `mdx-components.tsx` styles are active here too.

- Global `h1` style
- Global `p` and `ul` styles
- Custom `Callout` component

