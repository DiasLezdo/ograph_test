# Feeling MDX Components

<Callout type="info">
  If this box looks styled, your custom `Callout` from `mdx-components.tsx` is active.
</Callout>

This paragraph is rendered as `p` and styled globally through `mdx-components.tsx`.

## Link and code style

Visit the [SEO page](/seo) to compare regular page content.

Inline code sample: `const mode = "mdx";`

```ts
function helloMdx(name: string) {
  return `MDX says hi to ${name}`;
}
```

> Blockquotes are also mapped in `mdx-components.tsx`.

